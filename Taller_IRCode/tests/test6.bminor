/* Eliminación de Temporales Muertos */
Point: class = {
    x: float;
    y: float;
}

main: function void() = {
    p: Point = new Point(1.2, 0.6);
    px: float = p.x;
    py: float = p.y;
    sum: float = px + py;
    print px, py;
}